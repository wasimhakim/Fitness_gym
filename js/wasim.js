class Wasim {
    
    // random number generator
    random(from, to) {
    var randomNum = Math.floor((Math.random() * to) + from);
    return randomNum;
    }
}

var W = new Wasim();



